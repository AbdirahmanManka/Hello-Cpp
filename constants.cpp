// cpp constants
#include <iostream>
using namespace std;
//constants are like variables except that their value cannot be changed after their definition.
//To define a constant, we use the const keyword.
//To use constants, we have to declare them first.


int main() {
  const int myNum = 15;
  cout << myNum;
  return 0;
}